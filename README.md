# KRANSX

KRANSX is a Python package for authenticated byte envelopes on Python 3.10 and
newer.

```bash
python -m pip install kransx
```

The `kransx` command creates keys and seals or opens files without overwriting output.
